import React from 'react';
// import { useCharactersRequest } from '~utils/hooks/rickMortyHooks';

const RickMortyPage: React.FC = () => {
  // const [characters, paginationProps, loading, error] = useCharactersRequest();

  // console.log(characters);

  return null;
};

export default RickMortyPage;
